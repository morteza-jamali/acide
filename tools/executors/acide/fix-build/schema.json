{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "cli": "nx",
  "properties": {
    "outputPath": {
      "type": "string",
      "description": "build output path"
    },
    "replaceFiles": {
      "type": "array",
      "description": "replace files",
      "items": {
        "$ref": "#/definitions/replaceFiles"
      }
    }
  }
}
