---
labels: ['react', 'typescript', 'checkable-contextual-menu']
description: 'A CheckableContextualMenu component.'
---

import { CheckableContextualMenu } from '.';

CheckableContextualMenu example:

A simple CheckableContextualMenu component with some text

Code Snippet:
```js
<CheckableContextualMenu>hello from CheckableContextualMenu</CheckableContextualMenu>
```


Live Playground:
```js live
<CheckableContextualMenu>hello from CheckableContextualMenu</CheckableContextualMenu>
```
